// Fuente única de verdad: los colores están definidos en @/types
// Este archivo existe solo por compatibilidad con imports legacy
export { COLORS } from '@/types';
export { COLORS as default } from '@/types';
